# START HERE

如果你是一个新的 ChatGPT / Codex 会话，请先阅读本文件，再根据当前任务按需读取本仓库中的其他文档。

## 这个仓库的作用

`chat-the-best` 是跨会话共享的长期上下文仓库。

这里保存的是已经整理过、未来仍然有价值的信息，而不是完整聊天记录。

## 新会话工作方式

1. 先理解用户当前要解决的问题，不要机械读取整个仓库。
2. 如果当前问题涉及既有项目、历史决策、环境约束或之前已经讨论过的方案，优先在本仓库中查找相关 Markdown 文档。
3. 使用仓库中的总结作为上下文，但如果内容和实际项目仓库、当前代码或最新事实冲突，以最新、可验证的信息为准。
4. 不要因为仓库里存在历史方案，就默认它仍然有效；必要时确认其状态。
5. 当本次讨论形成值得长期保留的新结论时，整理后更新本仓库，而不是直接保存聊天流水。

## 快捷口令

当用户说：

> 沉淀到通用仓库

将其视为以下固定指令：

1. 总结当前会话中值得长期保留的背景、结论、决策、约束和阶段性状态。
2. 不机械复制聊天记录，只保留未来新会话真正需要知道的信息。
3. 自行判断应更新已有 Markdown 文档，还是新建合适的 Markdown 文档。
4. 总结类内容不要创建 Issue。
5. 如果会话中出现明确的待推进任务，可以在总结中指出它属于哪个项目、建议形成什么 Issue，但不要因为执行本口令而自动创建 Issue。
6. 完成后向用户说明新增或更新了哪些文件，以及各文件记录了什么。

如果当前会话尚未明确 `chat-the-best` 仓库位置，则本口令默认指向：`shenxg13/chat-the-best`。

## Issue 与文档的边界

### 应该创建 Issue

Issue 用于需要被推进到完成的事项，例如：

- 一个明确需求
- 一个 bug
- 一个需要实现或验证的技术任务
- 一个有完成状态的调查项

这类 Issue 通常应创建在实际所属的项目仓库中，而不是 `chat-the-best`。

### 不需要创建 Issue

以下内容直接作为 Markdown 文档维护：

- 会话总结
- 项目背景
- 架构说明
- 技术知识整理
- 已确认决策及其原因
- 环境信息和长期约束
- 跨项目的方法论

## 记录内容时的原则

优先记录：

- 最终结论
- 为什么这样决定
- 当前有效状态
- 关键约束
- 后续继续讨论时必须知道的信息

尽量不要记录：

- 没有价值的聊天过程
- 已被推翻且没有历史意义的中间猜测
- 可以随时从代码或官方文档直接得到的信息
- 密码、Token、私钥等敏感信息

## 推荐的后续目录

随着内容增加，可以逐步建立：

```text
projects/     # 各项目长期上下文
topics/       # PostgreSQL、TiDB、Kubernetes 等专题总结
decisions/    # 重要决策记录
summaries/    # 阶段性或时间维度总结
```

目录不需要提前建全，出现实际内容时再创建即可。
